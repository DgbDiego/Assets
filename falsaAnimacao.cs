using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class falsaAnimacao : MonoBehaviour
{

    public float _escalaOriginal;
    public float _escalaDesejada;
    public GameObject _objFilho;    


    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
